custom:
  stage: ${opt:stage, self.provider.stage, 'dev'}
  pythonRequirements:
    dockerizePip: non-linux

package:
  exclude:
    - ".**"
    - ".**/**"
    - "**/.__pycache__/**"
    - "**/.serverless/**"
    - node_modules/**
    - pogam.egg-info/**
